<Window x:Class="MidiHub.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MidiHub" Height="560" Width="660"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E" Closing="Window_Closing">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <!-- Style slider thème sombre -->
        <Style TargetType="Slider">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="#89B4FA" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="12" Height="12" Fill="#CDD6F4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="#45475A" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Statut MIDI -->
        <Border Grid.Row="0" Background="#313244" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="StatusDot" Width="12" Height="12" Fill="#F38BA8" Margin="0,0,10,0"/>
                    <TextBlock x:Name="StatusText" Text="Recherche du périphérique MIDI..." FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="LogToggleBtn" Content="Log ▼" HorizontalAlignment="Right"
                        Background="#45475A" Foreground="#A6ADC8" BorderThickness="0"
                        Padding="8,3" FontSize="12" Cursor="Hand"
                        Click="LogToggleBtn_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#45475A"/>
                            <Setter Property="Foreground" Value="#A6ADC8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#585B70"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Volume global -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="45"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Volume global" FontSize="13" Foreground="#A6ADC8" VerticalAlignment="Center"/>
                <Slider Grid.Column="1" x:Name="GlobalVolumeSlider"
                        Minimum="0" Maximum="1" Value="1"
                        ValueChanged="GlobalVolumeSlider_ValueChanged"/>
                <TextBlock Grid.Column="2" FontSize="12" Foreground="#A6E3A1" VerticalAlignment="Center" HorizontalAlignment="Right"
                           Text="{Binding ElementName=GlobalVolumeSlider, Path=Value, StringFormat={}{0:P0}}"/>
            </Grid>
        </Border>

        <!-- Liste des sons mappés -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Sons mappés" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,8"/>
                <ItemsControl x:Name="SoundsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#45475A" CornerRadius="5" Padding="10,6" Margin="0,0,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding ProgramLabel}" FontWeight="Bold" Foreground="#89B4FA" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding FileName}" Foreground="#CDD6F4" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0"/>
                                    <Slider Grid.Column="2"
                                            Minimum="0" Maximum="1"
                                            Value="{Binding Volume, Mode=TwoWay}"
                                            Margin="0,0,6,0"/>
                                    <TextBlock Grid.Column="3" FontSize="11" Foreground="#A6ADC8" VerticalAlignment="Center" HorizontalAlignment="Right"
                                               Text="{Binding VolumeLabel}"/>
                                    <TextBlock Grid.Column="4" Text="{Binding PlayState}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{Binding PlayColor}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Log MIDI -->
        <Border x:Name="LogPanel" Grid.Row="3" Background="#313244" CornerRadius="8" Padding="12" Visibility="Collapsed">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Log MIDI" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,8"/>
                <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LogText" FontFamily="Consolas" FontSize="12" Foreground="#A6E3A1" TextWrapping="Wrap"/>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Window>
